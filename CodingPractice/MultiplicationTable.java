public class MultiplicationTable {
    public static void main(String[] args) {
        System.out.println();
        int[][] smallTable = createMultiplicationTable(3);
        printMultiplicationTable(smallTable);
        
        System.out.println();
        int[][] largeTable = createMultiplicationTable(10);
        printMultiplicationTable(largeTable);
    }
    
    /**
     * Creates and returns a multiplication table witn n rows and n columns, such that
     * the table is a square array of width n which is passed into the method.
     *
     * @param n the number of rows and columns for the multiplication table to have.
     * @return // TODO: Complete the Javadoc
     */
    public static int[][] createMultiplicationTable(int n) {
        // TODO: Add code here
        
    }
    
    /**
     * Prints an array to the command window using a for loop and the printf method.
     * The format specifier %4d is used to create the table with consistent column sizes.
     *
     * @param table integer array to be printed to command window
     */
    public static void printMultiplicationTable(int[][] table) {
        // TODO: Add code here
        
    }
}
